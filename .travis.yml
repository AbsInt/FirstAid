sudo: required
dist: trusty
language: generic

install:
  - sudo add-apt-repository -y ppa:beineri/opt-qt57-trusty
  - sudo apt-get update
  - sudo apt-get -y install qt57tools qt57svg

script:
  - source /opt/qt57/bin/qt57-env.sh
  - cmake . && make
  - git clean -xfd
